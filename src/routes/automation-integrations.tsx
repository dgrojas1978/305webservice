import ServicePage from "~/components/pages/ServicePage";
export default function P() {
  return (
    <ServicePage locale="en" page="automation" metaKey="automation"
      offerIds={["automation"]}
      heroTitle="Digital Marketing & Automation"
      heroIntro="More leads, less busywork. Campaigns, review funnels and follow-ups that run on their own — connected to the tools you already use."
      heroService="automation" />
  );
}
